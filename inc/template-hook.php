<?php
// add_action( 'rwmb_profile_after_process', function( $config, $user_id ) {
// if ( 'dang-ky' === $config['id'] ) {
// if ( get_user_meta( $user_id, 'check_user' ) ) {
// $user  = new WP_User( $user_id );
// $email = $user->user_email;
// wp_mail( $email, 'Đăng ký thành công', 'A new user has been just submitted.' );
// }
// wp_safe_redirect( '' );
// die;
// }
// }, 10, 2 );
